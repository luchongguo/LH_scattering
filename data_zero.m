lent1=zeros(lengthray,nray);
beta11=zeros(lengthray,nray);
z1=zeros(lengthray,nray);
r1=zeros(lengthray,nray);
phi1=zeros(lengthray,nray);
cnz1=zeros(lengthray,nray);
cnr1=zeros(lengthray,nray);
cm1=zeros(lengthray,nray);
us1=zeros(lengthray,nray);
Npar1=zeros(lengthray,nray);
teta1=zeros(lengthray,nray);
sbr=zeros(lengthray,nray);
sbz=zeros(lengthray,nray);
sbt=zeros(lengthray,nray);
nperps1=zeros(lengthray,nray);
nperps11=zeros(lengthray,nray);
nperpf1=zeros(lengthray,nray); 
nperpf11=zeros(lengthray,nray);
ste=zeros(lengthray,nray);
sti=zeros(lengthray,nray);
dddd1=zeros(lengthray,nray);
ddt1=zeros(lengthray,nray);
ii1=zeros(lengthray,nray);
jj1=zeros(lengthray,nray);
sne1=zeros(lengthray,nray);
snperm1=zeros(lengthray,nray);
rhopsi1=zeros(lengthray,nray);
cnteta1=zeros(lengthray,nray);